name: C compile and run testing
on: [push]
jobs:
  root-dir:
    runs-on: ubuntu-latest
    steps: 
      - name: Where am I?
        run : pwd
        run : ls
      - name: Compile C in root folder
        run: gcc ${{ github.workspace }}/root_test.c -o main
      - run: echo "🎉 Compile successful"
      - name: Run C in root folder
        run: ./main
      - run: echo "🎉 Run successful"

  folder-dir:
    runs-on: ubuntu-latest
    steps: 
      - name: Move to folder
        run : cd ${{ github.workspace }}/folder1
        run : ls
      - name: Compile C in sub folder
        run: gcc folder_test.c -o main_sub
      - run: echo "🎉 Compile successful"
      - name: Run C in sub folder
        run: ./main_sub
      - run: echo "🎉 Run successful"